# Do not edit this file directly. Use a ‘docker-compose.override.yaml’ file if you can.
# Refer to `docker-compose.override.yaml.example’ for some sample configurations.

services:
  api:
    container_name: nxsGPT
    ports:
      - "${PORT}:${PORT}"
    depends_on:
      - mongodb
      - rag_api
    build:
      context: .
      dockerfile: Dockerfile.multi
      target: api-build
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - HOST=0.0.0.0
      - MONGO_URI=mongodb://root:example@mongodb:27017/nxsGPT?authSource=admin
      - MEILI_HOST=http://meilisearch:7700
      - RAG_PORT=${RAG_PORT:-8000}
      - RAG_API_URL=http://rag_api:${RAG_PORT:-8000}
      - CREDS_KEY=95f282342483eccef8321275a964c0a33b97759ec203ee1e5626739b4c52110d
      - CREDS_IV=af22a05a32d579d0529055314974c935
      - JWT_SECRET=443d49959580fd2380a77398581c00c1519e0f0472bd92ccaa785360f1e16c76
      - JWT_REFRESH_SECRET=eaa5191f2914e30b9387fd84e254e4ba6fc51b4654968a9b0803b456a54b8418
      - ALLOW_REGISTRATION=true
      - ALLOW_EMAIL_LOGIN=true
      - LOG_DIR=/app/logs
    volumes:
      - ./logs:/app/logs
      - type: bind
        source: ./.env
        target: /app/.env
      - ./librechat.yaml:/app/librechat.yaml
      - ./images:/app/client/public/images
      - ./client/public/assets:/app/client/public/assets
      - ./uploads:/app/uploads
      - ./logs:/app/api/logs
  mongodb:
    container_name: nxsgpt-mongodb
    image: mongo:5.0
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    volumes:
      - datanode:/data/db

  meilisearch:
    container_name: nxsgpt-meilisearch
    image: getmeili/meilisearch:v1.12.3
    restart: always
    environment:
      - MEILI_HOST=http://meilisearch:7700
      - MEILI_NO_ANALYTICS=true
      - MEILI_MASTER_KEY=${MEILI_MASTER_KEY}
    volumes:
      - meilidata:/meili_data
  vectordb:
    container_name: vectordb
    image: ankane/pgvector:latest
    environment:
      POSTGRES_DB: mydatabase
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    restart: always
    volumes:
      - pgdata2:/var/lib/postgresql/data
  rag_api:
    container_name: rag_api
    image: ghcr.io/danny-avila/librechat-rag-api-dev-lite:latest
    environment:
      - DB_HOST=vectordb
      - RAG_PORT=${RAG_PORT:-8000}
    restart: always
    depends_on:
      - vectordb
    env_file:
      - .env


volumes:
  pgdata2:
  datanode:
  meilidata:
